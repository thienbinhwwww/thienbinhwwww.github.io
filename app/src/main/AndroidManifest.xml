<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.learnforeignlanguage">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".ListVocabularyActivity"></activity>
        <activity android:name=".ElectiveActivity" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".GameIIActivity" />
        <activity android:name=".GameIActivity" />
        <activity android:name=".SelectGameActivity" />
        <activity android:name=".SignUpActivity" />
        <activity android:name=".ListGameActivity" />
        <activity android:name=".ThemeActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".UsesActivity" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>